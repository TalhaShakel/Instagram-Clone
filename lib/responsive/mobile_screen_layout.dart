import 'package:flutter/material.dart';

class mob_screen_layout extends StatelessWidget {
  const mob_screen_layout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Mobile screen"),
      ),
    );
  }
}
